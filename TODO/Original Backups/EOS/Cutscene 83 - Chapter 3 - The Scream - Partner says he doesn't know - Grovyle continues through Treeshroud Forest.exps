
def 0 {
    bgm_PlayFadeIn(BGM_TIME_GEAR_REMIX, 0, 256);
    bgm2_PlayFadeIn(BGM_STORM, 0, 256);
    back_SetGround(LEVEL_V03P11A);
    supervision_Acting(0);
    camera_SetMyself<performer 0>();
    SetAnimation<actor ACTOR_NPC_JUPUTORU>(42);
    SetEffect<performer 3>(EFFECT_VERY_HEAVY_RAIN, 3);
    back_SetBackScrollSpeed(-12.0, 0);
    bgm2_ChangeVolume(30, 190);
    screen_FadeIn(1, 15);
    Wait(30);
    message_SetActor(ACTOR_ATTENDANT1);
    message_SwitchTalk ($PARTNER_TALK_KIND) {
        case 1:
            {
                english="""
                     I don't really know the answer
                    to that question.
                """,
            }
        case 2:
            {
                english="""
                     I don't really know the answer
                    to that question.
                """,
            }
        default:
            {
                english="""
                     I don't really know the answer
                    to that question.
                """,
            }
    }
    message_Close();
    se_Play(6420);
    SetAnimation<actor ACTOR_NPC_JUPUTORU>(1024);
    MovePositionMark<actor ACTOR_NPC_JUPUTORU>(5, Position<'m0', 18, 29.5>);
    WaitExecuteLives(ACTOR_NPC_JUPUTORU);
    MovePositionOffset<actor ACTOR_NPC_JUPUTORU>(7, -100, 0);
    se_Stop(6420);
    se_Play(6421);
    supervision_Acting(1);
    Wait(10);
    screen_FadeOut(1, 5);
    end;
}
