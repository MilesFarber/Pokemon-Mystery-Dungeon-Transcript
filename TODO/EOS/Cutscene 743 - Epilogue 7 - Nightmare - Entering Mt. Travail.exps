
def 0 {
    sound_Stop();
    $SCENARIO_MAIN = scn[29, 74];
    back_SetGround(LEVEL_D36P11A);
    supervision_Acting(0);
    camera_SetMyself<performer 0>();
    screen_FadeIn(1, 30);
    bgm_PlayFadeIn(BGM_MT_TRAVAIL, 0, 256);
    MovePositionMark<actor ACTOR_ATTENDANT1>(1, Position<'m0', 23.5, 18.5>);
    MovePositionMark<actor ACTOR_PLAYER>(1, Position<'m1', 27.5, 18.5>);
    WaitExecuteLives(ACTOR_PLAYER);
    Wait(15);
    message_SetFace(ACTOR_ATTENDANT1, FACE_NORMAL, FACE_POS_STANDARD);
    message_SwitchTalk ($PARTNER_TALK_KIND) {
        case 1:
            {
                english=" So this is [CS:P]Mt. Travail[CR].",
            }
        case 2:
            {
                english=" So this is [CS:P]Mt. Travail[CR].",
            }
        default:
            {
                english=" This must be [CS:P]Mt. Travail[CR].",
            }
    }
    CallCommon(CORO_MESSAGE_CLOSE_WAIT_FUNC);
    Turn2DirectionLives<actor ACTOR_ATTENDANT1>(4, 10, ACTOR_PLAYER);
    WaitExecuteLives(ACTOR_ATTENDANT1);
    Turn2DirectionLives<actor ACTOR_PLAYER>(4, 10, ACTOR_ATTENDANT1);
    message_SwitchTalk ($PARTNER_TALK_KIND) {
        case 1:
            {
                english="""
                     We need [CS:N]Drowzee[CR] to help us
                    figure out why [CS:N]Azurill[CR] won't wake up.
                """,
            }
        case 2:
            {
                english="""
                     We need [CS:N]Drowzee[CR] to help us
                    figure out why [CS:N]Azurill[CR] won't wake up.
                """,
            }
        default:
            {
                english="""
                     We need [CS:N]Drowzee[CR] to help us
                    figure out why [CS:N]Azurill[CR] won't wake up.
                """,
            }
    }
    message_SwitchTalk ($PARTNER_TALK_KIND) {
        case 1:
            {
                english="""
                     Let's not waste any time finding
                    [CS:N]Drowzee[CR]!
                """,
            }
        case 2:
            {
                english="""
                     We have to hurry finding
                    [CS:N]Drowzee[CR]!
                """,
            }
        default:
            {
                english="""
                     There's no time to spare finding
                    [CS:N]Drowzee[CR]!
                """,
            }
    }
    message_SwitchTalk ($PARTNER_TALK_KIND) {
        case 1:
            {
                english="""
                     We have to try our best,
                    [hero]!
                """,
            }
        case 2:
            {
                english="""
                     We've got to be at our best,
                    [hero]!
                """,
            }
        default:
            {
                english="""
                     Let's do our very best,
                    [hero]!
                """,
            }
    }
    message_Close();
    MovePositionMark<actor ACTOR_PLAYER>(1, Position<'m2', 25.5, 10.5>);
    Wait(15);
    MovePositionMark<actor ACTOR_ATTENDANT1>(1, Position<'m3', 25.5, 10.5>);
    Wait(30);
    screen_FadeOut(1, 30);
    main_EnterDungeon(62, 0);
    switch ( message_Menu(MENU_DUNGEON_INITIALIZE_TEAM) ) { }
    main_EnterDungeon(-1, 0);
    hold;
    end;
}
