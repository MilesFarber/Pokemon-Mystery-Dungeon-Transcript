
def 0 {
    bgm_PlayFadeIn(BGM_SPINDAS_CAFE, 0, 256);
    back_SetGround(LEVEL_V37P01A);
    supervision_Acting(0);
    camera_SetMyself<performer 0>();
    MovePositionMark<performer 0>(0.5, Position<'m0', 16, 11.5>);
    screen_FadeIn(1, 60);
    WaitExecutePerformer(0);
    message_SetActor(ACTOR_NPC_BARIYAADO);
    message_Talk({
        english="""
             It's a really tall mountain to
            the east.
        """,
    });
    message_Talk({
        english="""
             It's said that the mountain is so
            tall, it almost reaches beyond the sky.
        """,
    });
    message_Talk({
        english="""
             But it's surrounded by an
            impassable mountain range, so the route there
            has been lost.
        """,
    });
    message_Talk({
        english="""
             That's why it hasn't been fully
            explored yet.
        """,
    });
    message_Close();
    screen_FadeOut(1, 60);
    end;
}
